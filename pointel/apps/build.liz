#!/bin/Liz

print("Building...")
if Liz.has("src/all.ts") then Liz.run("mkall") end
if Liz.is_win() then
    npm = "npm.cmd"
else
    npm = "npm"
end
Liz.cmd(npm, {"install"})
Liz.cmd("tsc", {"-p", "tsconfig.json"})
print("Done Build!")
