#!/bin/Liz
print("Building...")
Liz.run("mkall")
if Liz.is_win() then
  npm = "npm.cmd"
else
  npm = "npm"
end
Liz.cmd(npm, {"install"})
Liz.cmd("tsc", {"-p", "tsconfig.json"})
print("Done Build!")
