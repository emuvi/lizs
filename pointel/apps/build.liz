#!/bin/Liz

print("Building...")
if Liz.has("src/all.ts") then Liz.run("mkall") end
if Liz.is_win() then
    Call_npm = "npm.cmd"
else
    Call_npm = "npm"
end
Liz.cmd(Call_npm, {"install"})
Liz.cmd("tsc", {"-p", "tsconfig.json"})
print("Done Build!")
